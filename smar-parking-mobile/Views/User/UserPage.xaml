<ContentPage
  xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
  xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
  xmlns:local="clr-namespace:smar_parking_mobile"
   xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
  xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
  mc:Ignorable="d"
  d:DesignWidth="800"
  d:DesignHeight="450"
  x:Class="smar_parking_mobile.Views.UserPage">

  
  <VerticalStackLayout Spacing="20" Padding="30" VerticalOptions="Start">
       
        
        <Image Source="user_not_logged.png" HeightRequest="100" />
        <Label Text="Access Required" 
               FontSize="24" 
               HorizontalOptions="Center" />

        <Grid>
          <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
          </Grid.RowDefinitions>
          <Grid.ColumnDefinitions>
            <ColumnDefinition></ColumnDefinition>
          </Grid.ColumnDefinitions>
          <VerticalStackLayout Grid.Row="0" Spacing="5">
                <Label Text="Email" />
                <Entry x:Name="EmailEntry" 
                       Placeholder="john.doe@outlook.it" 
                       Keyboard="Text" />
            </VerticalStackLayout>

            <VerticalStackLayout Grid.Row="1" Spacing="5">
                <Label Text="Password" />
                <Entry x:Name="PasswordEntry" 
                       Placeholder="Password" 
                       Keyboard="Password" />
            </VerticalStackLayout>
        </Grid>
        <Button Text="Log In"
                    Clicked="OnSubmitClicked"
                    BackgroundColor="#333F69"
                    TextColor="White"
                    Margin="0,20,0,0" />
    </VerticalStackLayout>
</ContentPage>